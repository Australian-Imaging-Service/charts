apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monai-label.fullname" . }}-test-connection"
  labels:
    {{- include "monai-label.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monai-label.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
