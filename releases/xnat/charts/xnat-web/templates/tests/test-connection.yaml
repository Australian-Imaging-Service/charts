apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xnat-web.fullname" . }}-test-connection"
  labels:
    {{- include "xnat-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  securityContext:
    {{- toYaml .Values.podSecurityContext | nindent 4 }}
  containers:
    - name: wget
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      image: busybox
      command: ['wget']
      args: ['{{ include "xnat-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
