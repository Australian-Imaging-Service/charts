#
# xnat-conf.properties
# XNAT http://www.xnat.org
# Copyright (c) 2016, Washington University School of Medicine
# All Rights Reserved
#
# Released under the Simplified BSD.
#
datasource.driver=org.postgresql.Driver
datasource.url=${PGJDBCURI}
datasource.username=${PGUSER}
datasource.password=${PGPASSWORD}
hibernate.dialect=org.hibernate.dialect.PostgreSQL9Dialect
hibernate.hbm2ddl.auto=update
hibernate.show_sql=false
hibernate.cache.use_second_level_cache=true
hibernate.cache.use_query_cache=true
{{- if or .Values.activemq.enabled .Values.activemq.auth.existingSecret }}
spring.activemq.broker-url=${AMQBROKERURL}
spring.activemq.user=${AMQUSERNAME}
spring.activemq.password=${AMQPASSWORD}
{{- end }}
